# AI 代码审查检查清单（Checklist）

> **用途**：本清单随业务代码一起提供给 AI 审查模型。模型须**按流程、逐项核对代码实现**，
> 每项给出「通过 / 不通过」结论；不通过时必须指出**文件位置（文件:行号）**、问题原因与严重级别
> （严重 / 重要 / 一般）。检查重点是：功能正确性、状态机合法性、边界与异常处理、安全缺陷、
> 并发竞态、敏感数据保护、审计日志完整性。

---

## 1. 注册流程

- [ ] 用户名格式校验：仅允许 3~32 位字母/数字/下划线，非法输入返回 400
- [ ] 密码强度校验：至少 8 位且同时包含字母和数字，弱密码返回 400
- [ ] 手机号格式校验：11 位、1 开头，非法返回 400；手机号可空时降级逻辑正确
- [ ] 用户名唯一性：已占用返回 409，不返回 500
- [ ] 手机号唯一性：已被注册返回 409，不返回 500
- [ ] 注册用短信验证码：手机号非空时必须校验 `purpose=register` 的验证码，且校验在创建用户**之前**
- [ ] 密码以 bcrypt（cost=12）哈希入库，绝不存明文
- [ ] 注册成功写审计日志（`login_type=register`, `success=1`）
- [ ] 注册后自动签发会话（登录态）的行为与产品预期一致
- [ ] 并发注册同一用户名/手机号时，数据库唯一约束冲突被捕获并返回 409，而非 500
- [ ] 错误提示不泄露内部细节，状态码语义化（400/409）

## 2. 短信验证码流程

- [ ] 发送频控：同一手机号 60 秒内最多 1 条，超出返回 429
- [ ] 每日上限：同一手机号当天（00:00:00 起）最多 10 条，超出返回 429
- [ ] 验证码为 6 位数字，用 `random_int` 生成（非 `rand`/`mt_rand`）
- [ ] 验证码只存 `SHA256(code + SMS_SALT)` 哈希，绝不落明文
- [ ] `debug_code`（明文）仅当 `SMS_DEBUG=true` 时写入数据库并返回
- [ ] 验证码有效期 5 分钟（300 秒），过期作废
- [ ] 校验使用 `hash_equals` 常数时间比较，防时序攻击
- [ ] 校验条件完整：`purpose` 匹配、`used=0`、未过期，且取最新一条记录
- [ ] 5 次校验错误后验证码作废（`attempt_count` 上限）
- [ ] 校验通过后标记 `used=1`，验证码一次性、不可重放
- [ ] `login` / `register` / `bind` 三种用途互不通用
- [ ] 发送接口不泄露账号是否存在（不查库或对已注册/未注册手机号响应一致）
- [ ] 频控窗口的时间源一致（应用 `date()` 与数据库 `created_at` 无时区错位）

## 3. 密码登录流程（防爆破与锁定）

- [ ] 支持用户名/手机号两种账号形式登录
- [ ] 账号不存在时返回与密码错误完全一致的 401 文案「用户名或密码错误」，不泄露账号是否存在
- [ ] 账号不存在时也写审计日志（`reason=user_not_found`）
- [ ] 锁定检查先于密码校验：锁定中的账号直接返回 423
- [ ] IP 限流：单 IP 10 分钟内 20 次失败返回 429
- [ ] 密码错误后 `failed_attempts` 自增并落库
- [ ] 连续 5 次失败触发锁定 15 分钟（`locked_until`），且锁定后计数清零
- [ ] 锁定期间继续输错不再累积计数（或行为有明确定义）
- [ ] 登录成功清空失败计数与锁定状态
- [ ] 成功/失败均写审计（`reason`: `ok` / `wrong_password` / `locked`）
- [ ] 失败 401、锁定 423、限流 429，状态码语义化且文案明确
- [ ] 并发错误尝试下 `failed_attempts` 的读写无竞态（非原子自增可能绕过锁定）
- [ ] 用户不存在与密码错误两个分支的响应耗时差异不构成可探测的时序侧信道

## 4. 短信验证码登录流程

- [ ] 先校验验证码（`purpose=login`）再查询用户，验证码一次性使用
- [ ] 未注册手机号返回 404「请先注册」，且不泄露验证码有效性以外的信息
- [ ] 验证码校验失败不影响账号状态（不消耗密码失败计数）
- [ ] 已锁定账号禁止短信登录（走与密码登录相同的锁定检查）
- [ ] 登录成功写审计（`login_type=sms`, `success=1`）
- [ ] 登录成功签发会话并绑定设备（`issueSession`）
- [ ] 校验失败/成功的状态码与文案语义化（400/404/423/429）

## 5. 扫码登录流程（QR 票据）

- [ ] QR ID 为 8 位随机字符（去除易混淆字符集），`random_int` 生成，不可预测、碰撞概率可接受
- [ ] 票据有效期 120 秒，过期后不可确认、不可领取令牌
- [ ] 状态机合法：`pending → confirmed → used` 或 `pending → expired`，无非法跳转
- [ ] 确认接口受登录鉴权保护（已登录手机端才能确认）
- [ ] 确认时校验：`qr_id` 存在、`status=pending`、未过期，任一不满足返回 400
- [ ] 确认记录 `user_id` / `device_id` / `confirmed_at`
- [ ] 一个二维码只能被确认一次、只能被领取一次令牌（`used` 后不可复用）
- [ ] 轮询返回当前状态正确（`pending` / `expired` / `confirmed` / `used`）
- [ ] 轮询在 `confirmed` 后签发令牌并原子地标记 `used`
- [ ] 并发轮询同一二维码时不会重复签发令牌（需原子更新或唯一性兜底）
- [ ] 过期票据在轮询时惰性更新为 `expired`
- [ ] `qr_id` 输入归一化（大写化）与生成逻辑一致
- [ ] 扫码登录成功写审计（`login_type=qr`）
- [ ] 非法/未知 `qr_id` 返回 400，不抛 500
- [ ] 确认端与轮询端设备标识（`device_id`）的取值与兜底逻辑正确

## 6. 令牌刷新流程（Refresh Token 轮换）

- [ ] `refresh_token` 仅从 httpOnly Cookie 读取，不接受请求体/参数
- [ ] 通过 `refresh_token_hash` 精确匹配，且 `revoked=0`
- [ ] 每次刷新签发全新 access + refresh，旧 refresh token 立即失效（轮换）
- [ ] 刷新后更新设备记录的令牌哈希与 `last_active_at`
- [ ] 账号被禁用（`status≠1`）时刷新返回 401
- [ ] 无效/已吊销/已轮换的 refresh token 返回 401
- [ ] 刷新成功写审计（`login_type=refresh`）
- [ ] 旧 refresh token 重放（reuse）有检测与告警，最好吊销整个设备会话
- [ ] 有效期配置正确：access 30 分钟、refresh 14 天，且 Cookie `Expires` 与之匹配
- [ ] 令牌 Cookie 属性正确：httpOnly、SameSite=Lax、path=/（生产另需 Secure）
- [ ] 设备被踢后其 refresh token 立即失效（`revoked=1` 查不到）

## 7. 登出流程

- [ ] 无 refresh token 时幂等返回成功，不抛异常
- [ ] 登出吊销当前设备（`revoked=1` 并清空 `refresh_token_hash`）
- [ ] 登出后 access/refresh Cookie 被清除
- [ ] 登出是否写审计日志（当前实现未记录——若安全审计要求"登出可追溯"，此处为缺失项）
- [ ] 登出后旧 access token 在过期前（最长 30 分钟）仍可访问受保护接口——该行为是否有明确定义

## 8. 设备管理流程

- [ ] 设备列表仅返回当前用户（`user_id` 过滤），他人设备不可见
- [ ] 设备列表响应不泄露 `refresh_token_hash`（模型 `hidden` 配置生效）
- [ ] 踢单台设备前校验该设备属于当前用户，否则 404/403
- [ ] 禁止踢出当前设备（返回 400 或明确定义）
- [ ] 踢出操作：`revoked=1` + `refresh_token_hash` 置空，踢出后该设备 refresh 立即失效
- [ ] 一键踢全部（`kick-others`）排除当前设备
- [ ] `device_id` 取自 `X-Device-Id` 头，有长度截断（64）与合法性校验，无注入风险
- [ ] 设备名解析（UA 解析）对畸形 User-Agent 不抛异常
- [ ] 列表按 `last_active_at` 降序，分页参数有上限约束

## 9. 审计日志流程

- [ ] 审计事件覆盖所有认证路径：注册、密码登录、短信登录、扫码登录、刷新（登出缺失需标注）
- [ ] 审计字段完整：`user_id` / `username` / `login_type` / `success` / `reason` / `ip` / `user_agent` / `device_id`
- [ ] `clientIp` 对 `X-Forwarded-For` 的信任边界明确：该头可被客户端伪造，需确认代理层清洗后才可信任（否则 IP 风控与审计可被绕过）
- [ ] IP 限流触发（429）时是否写入审计（`reason=rate_limited`）——当前 `checkIp` 抛异常前不记录，此为缺口
- [ ] 失败原因分类正确：`wrong_password` / `locked` / `rate_limited` / `user_not_found` / `account_disabled`
- [ ] 汇总接口统计口径正确：24 小时窗口、`blocked` = `locked` + `rate_limited`、成功率计算
- [ ] 日志分页：`per_page` 有上下限（当前 5~50），非法参数被兜底
- [ ] `sms-codes` 演示查询接口仅在 `SMS_DEBUG=true` 时可用（否则 403）
- [ ] 审计表只追加不修改删除，无长期运行膨胀与清理策略
- [ ] 审计日志无防篡改机制（哈希链/签名）——按合规要求评估

## 10. KYC 实名认证流程（L0 → L3）

- [ ] L0→L1 绑手机：格式校验、手机号唯一（409）、验证码 `purpose=bind` 校验
- [ ] L1 级别取 `max(当前级别, 1)`，不会降级
- [ ] L1→L2 实名：已有 ≥L2 认证时拒绝重复认证（400）
- [ ] 真实姓名长度校验（2~32 字符）
- [ ] 身份证 GB11643 校验完整：18 位格式、出生日期合法性（`checkdate`）、17 位加权校验位算法正确
- [ ] 身份证原文不落库，仅存脱敏值（3 位 + `***********` + 末 4 位）
- [ ] 人脸照片上传：格式白名单（jpeg/png/webp）、大小 ≤5MB，超出返回 400
- [ ] 人脸比对分数阈值（0.85）生效，低于阈值拒绝
- [ ] 活体/比对分数来源可信（Mock 基于文件内容哈希，生产需真实活体检测，不得由客户端传入）
- [ ] 照片文件名服务端生成（用户 id + 时间 + 随机数），不信任客户端文件名，无路径穿越
- [ ] 上传目录权限收紧；照片仅按声明周期保留（注释声称"模拟即删"，需核对实际清理逻辑——隐私合规 PIPL）
- [ ] 文件内容真实性校验（仅检查 `Content-Type` 头可伪造，至少应验魔数）
- [ ] L2→L3 四要素：级别门槛（<L2 拒绝）、卡号 16~19 位数字、Luhn 校验、预留手机号与绑定手机号一致
- [ ] 敏感字段存储符合 schema 注释承诺（`real_name` 注释称"加密存储"，需核对实际是否加密）
- [ ] 认证记录可追溯：每级一条 `kyc_records`，`provider` 标记核验来源
- [ ] 所有 KYC 接口均在登录鉴权中间件保护下
- [ ] 绑手机时验证码只能使用一次，校验后即作废

## 11. 安全基础与中间件

- [ ] CSRF 双提交：所有写请求（POST/PUT/PATCH/DELETE）校验 `X-CSRF-Token` 头与 Cookie 一致，`hash_equals` 比较，失败返回 419
- [ ] CSRF Cookie 属性：非 HttpOnly（JS 可读）、SameSite=Lax、7 天有效期
- [ ] 鉴权中间件：JWT 缺失/解析失败/无 `sub` 均返回 401 JSON
- [ ] JWT 签发：HS256、含 `iss`/`sub`/`iat`/`exp`/`jti`，有效期 30 分钟，签名密钥为强随机
- [ ] 生产环境无默认/硬编码密钥：`JWT_SECRET`、`SMS_SALT`、数据库密码均可配置且生产必须更换
- [ ] 安全响应头齐全：CSP、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`Permissions-Policy`
- [ ] 统一异常处理：5xx 响应不返回堆栈与内部细节
- [ ] 未匹配路由返回 JSON 404，而非 HTML
- [ ] 登录令牌 Cookie 属性：httpOnly、SameSite=Lax（生产 HTTPS 下加 Secure）
- [ ] 审计日志与响应中绝不含明文密码与验证码
- [ ] 所有 SQL 走 ORM 参数绑定，无字符串拼接注入面
- [ ] 密码哈希强度 bcrypt cost=12，验证码加盐哈希（`SMS_SALT`）

## 12. 通用检查（跨流程）

- [ ] 竞态审查：注册唯一性、二维码一次性使用、失败计数自增、验证码 `used` 标记——均需原子性保障
- [ ] 时区一致性：应用 `date()` 与数据库 `DATETIME` 使用同一时钟源，无 8 小时错位类隐患
- [ ] 输入归一化一致：所有入口对字符串 trim、类型强转（`(string)`/`(int)`）保持一致
- [ ] 分页/列表查询无 N+1 与全表扫描隐患
- [ ] 所有异常路径返回语义化状态码，无未捕获异常冒泡成 500
- [ ] 多表写入（如创建用户 + 写审计）失败时的一致性：是否需要事务/补偿
- [ ] 接口幂等性：重复提交（连点、重试）不产生重复副作用（重复注册、重复扣减等）
- [ ] 业务阈值/TTL 集中定义为常量（`QR_TTL_SECONDS`、`MAX_FAILED_PER_ACCOUNT`、`TTL_SECONDS` 等），无散落魔法数字
- [ ] 注释与实现一致：schema/注释声称的"加密存储""模拟即删""绝不外泄"须与实际代码行为对照核实
- [ ] 敏感配置（密钥、盐、调试开关）不提交到代码库与日志
- [ ] 错误文案面向用户不泄露内部实现细节（框架、库、SQL 错误）

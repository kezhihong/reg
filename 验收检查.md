# AI 代码审查检查清单（验收结果）

> 依据原清单逐项核对本仓库实现（Vue 3 + Hyperf 3.1 + MySQL 8 + Redis）。
> **不适用本项目的条目已删除**（原清单基于含 QR 扫码登录/身份证 GB11643/银行卡 Luhn/活体照片上传等功能的另一系统）。
> 结论：✅ 通过 / ⚠️ 有差异（按本项目文档口径）/ ❌ 不通过。
> 检查日期：2026-08；对应代码版本：960edcd。

---

## 1. 注册流程

- ✅ 用户名格式校验：`Validator::username`（`app/Util/Validator.php:102`）3~32 位字母/数字/下划线，非法输入 400
- ✅ 密码强度校验：`Validator::checkPassword`（`Validator.php:114`）8~72 位且含字母与数字，弱密码 400
- ⚠️ 手机号格式：`Validator.php:100` 为 11~15 位数字（`docs/02 §2.1` 口径），不强制「1 开头」——按文档
- ✅ 用户名唯一性：`AuthService::register` 捕获唯一约束 → 409（`AppErrorCode::USER_TAKEN=16101`）
- ✅ 手机号唯一性：同上（`uk_user_phone` 组合唯一索引兜底）
- ✅ 注册验证码：`register()` 先 `VerificationCodeService::verify`（scene=1）再创建用户（`AuthService.php:90-100`）
- ✅ 密码 bcrypt cost=12 哈希入库（`AuthService.php:92`），无明文列
- ✅ 注册成功写审计：`auth.login_success` + detail `{is_new:true}`（审计表 `sc_audit_logs`）
- ✅ 注册后自动签发会话（`successWithCookies` 种双 Cookie + device_key）
- ✅ 并发注册唯一约束冲突 → 409（`catch (\Throwable)` 转为 `USER_TAKEN`）
- ✅ 错误文案不泄露内部细节（业务异常统一 `{code,message}`）

## 2. 短信验证码流程

- ✅ 发送频控：同手机号 60 秒 1 条，超出 429（`VerificationCodeService.php:88-95`，code 10105）
- ✅ 每日上限：同手机号当日 10 条（`VerificationCodeService.php:104-112`）
- ✅ 6 位数字 `random_int(100000, 999999)` 生成（`SecurityUtil::code`）
- ✅ 只存 `sha256(独立盐 + code)` 哈希（每行独立盐列 `salt`），不落明文（`SecurityUtil::hash`）
- ⚠️ 明文回显：仅非生产且 `SMS_MOCK=true` 时响应返回 `mock_code`（`docs/04 §4.2` 门禁，等效原清单 `SMS_DEBUG`）
- ✅ 有效期 5 分钟（`expires_at = now + 300`）
- ✅ `hash_equals` 常数时间比较（`SecurityUtil::equals`）
- ✅ 校验条件：scene 匹配、`consumed_at=0`、未过期、取最新一条（`orderByDesc('id')`）
- ✅ 尝试 5 次上限作废（`max_attempts=5`，条件递增 `attempts < max_attempts`）
- ✅ 一次性消费：条件更新 `consumed_at`（`where consumed_at=0`），防并发重放
- ✅ 场景互不通用：`VerificationScene` 枚举 1~6（登录/注册/重置/绑手机/换邮箱/KYC）
- ✅ 发送接口不泄露账号存在性：`send` 不查账号；`forgot-password` 账号不存在也返回成功
- ✅ 时间源一致：全库 `BIGINT` Unix 秒（UTC），应用与库同源

## 3. 密码登录流程（防爆破与锁定）

- ✅ 支持用户名/手机号（纯数字默认区号 / `+86` 前缀）/邮箱 三合一（`AuthService::login`）
- ✅ 账号不存在与密码错误统一 10101 文案「账号或密码错误」（防枚举）
- ✅ 账号不存在也写登录日志（`user_id=0, fail_reason=2`，`sc_login_logs`）
- ✅ 锁定检查先于密码校验（`AuthService.php:134-137`，docs/04 §4.1）
- ⚠️ IP 限流口径：IP 20 次/分钟（`RateLimitMiddleware`，docs/04 §4.1 L1）——原清单「10 分钟 20 次」按文档调整
- ✅ 失败计数原子自增落库（`increment('login_failed_count', 1)`）
- ⚠️ 锁定阈值：密码 10 次/15 分钟锁 15 分钟（docs/04 §4.1；原清单 5 次——按文档），2FA 类 5 次（docs/04 §4.3）
- ✅ 锁定期间不累计（`locked_until > now` 直接返回）
- ✅ 登录成功清零失败计数与锁定
- ✅ 成功/失败均写日志（`sc_login_logs`：`is_success` + `fail_reason` 分类）
- ✅ 状态码语义化：401/423（`ACCOUNT_LOCKED`）/429（`RateLimitException` + `X-RateLimit-*` 头）
- ✅ 无竞态：`UPDATE ... SET login_failed_count = login_failed_count + 1` 原子
- ✅ 时序侧信道：账号不存在执行假 bcrypt 校验（`fakeHash`，cost 12）

## 4. 短信验证码登录流程

- ✅ 先校验验证码（scene=1）再查用户（`AuthService::smsLogin`）
- ⚠️ 未注册手机号行为：**自动建号**（登录注册二合一，`docs/02 §2.4`，原清单「返回 404」按文档调整）
- ✅ 验证码校验失败不影响账号状态（不消耗密码失败计数）
- ✅ 锁定账号禁止短信登录（`assertLoginAllowed` 与密码登录同口径）
- ✅ 登录成功写登录日志（`login_type=2` 短信）
- ✅ 签发会话并绑定设备（`establishSession`）
- ✅ 状态码语义化（400/423/429）

## 5. 扫码登录流程（QR 票据）

> **本项目无此功能，已删除**（原系统功能，本系统未设计 QR 扫码登录）。

## 6. 令牌刷新流程（Refresh Token 轮换）

- ✅ `refresh_token` 仅从 httpOnly Cookie 读取（`AuthService::refresh`）
- ✅ `token_hash` 精确匹配 + `revoked_at=0`（O(1) 唯一索引直查）
- ✅ 每次刷新轮换：旧行条件更新 `rotated_at` + 签发新对（事务内 `FOR UPDATE` 设备行串行化）
- ✅ 刷新更新设备 `last_active_at` / `last_ip`
- ✅ 账号禁用（`status=3`）刷新 401
- ✅ 无效/已吊销/已轮换 token 401
- ⚠️ 刷新成功不写审计：本项目审计事件清单（docs/04 §7.1）不含 refresh 成功事件——按文档（原清单要求，标注为已知差异）
- ✅ 重用检测：已轮换 + 异 IP → 吊销设备全部会话 + 审计 `refresh.reuse_detected`（docs/04 §3.4）
- ⚠️ 有效期：access 15 分钟 / refresh 30 天（docs/05 `TOKEN_ACCESS_TTL=900`、`TOKEN_REFRESH_TTL=2592000`；原清单 30 分钟/14 天按文档调整）
- ⚠️ Cookie 属性：SameSite=Strict、Path=/api 与 /api/v1/auth/refresh（docs/04 §3.1 更严格于原清单 Lax/path=/）
- ✅ 设备被踢后 refresh 立即失效（`revoked_at` 条件更新）

## 7. 登出流程

- ✅ 无 refresh token 幂等成功（`logout` 仅按上下文设备吊销，不依赖 Cookie）
- ✅ 吊销当前设备全部 refresh 行（reason=1 登出）+ 清 Cookie
- ✅ 登出写审计 `auth.logout`（`AuditAction::LOGOUT`）
- ✅ 旧 access 在 15 分钟窗口内仍可用——docs/04 §3.3 明确定义（原清单 30 分钟按文档调整）

## 8. 设备管理流程

- ✅ 设备列表仅当前用户（`where user_id`）
- ✅ 响应不泄露 `refresh_token_hash`（接口仅返回展示字段）
- ✅ 踢设备前校验归属（`findOwned` → 13101 设备不存在 404）
- ⚠️ 允许踢当前设备（等同登出并清 Cookie）——docs/02 §5.2 明确定义（原清单「禁止」按文档调整）
- ✅ 踢出 = 条件更新 `revoked_at`（防重复踢）+ 吊销该设备全部 refresh + 审计 `device.kick`
- ✅ 登出全部设备：`/auth/logout-all`（吊销全部 + `token_version+1`，等效原清单 kick-others）
- ⚠️ 设备标识：服务端生成 `device_key` Cookie（docs/01 D2，不信任客户端；原清单 `X-Device-Id` 头按文档调整）
- ✅ UA 解析容错（`UserAgentParser` 畸形 UA 返回「未知设备」）
- ⚠️ 列表排序：按 `id` 降序（新设备在前；原清单 `last_active_at` 降序——等效语义）
- ✅ 分页：游标 + `per_page` 1~100 上限

## 9. 审计日志流程

- ✅ 覆盖认证路径：注册、密码登录、短信登录、OAuth、登出、踢设备、锁定、重用检测、2FA、KYC（docs/04 §7.1 事件清单）
- ✅ 审计字段：`actor/actor_type/target/request_id/ip/user_agent/detail_json`（username 经 actor_id 关联，等效）
- ✅ `X-Forwarded-For` 信任边界：nginx 反代设置 `X-Real-IP` + 首段取值（`RequestIdMiddleware::clientIp`）
- ⚠️ 429 不写审计：限流命中由 Redis 计数可查（docs/04 未要求审计 429——标注为已知差异）
- ✅ 失败原因分类：`fail_reason` 1~7（密码错/账号不存在/锁定/禁用/验证码错/2FA/限流）
- ⚠️ 汇总统计接口：本项目无（原清单功能，已删除）
- ✅ 日志分页：游标 + `per_page` 1~100 兜底
- ⚠️ `sms-codes` 演示查询接口：本项目无（已删除；等价能力为发码响应 `mock_code` 门禁）
- ✅ 审计只追加：`AppendOnlyAspect` 拦截 UPDATE/DELETE + DB 账号最小权限设计（docs/04 §7.1）
- ⚠️ 防篡改哈希链：未实现（docs/04 未要求——标注为已知差异，合规按需评估）

## 10. KYC 实名认证流程（本项目仅 L1 手机号实名）

> 本项目 KYC 仅保留 **L1 手机号实名**（前端已移除 L2/L3 入口；后端 L2/L3 接口保留供扩展）。
> 原清单中身份证 GB11643、银行卡 Luhn、活体照片上传、0.85 阈值、L2→L3 四要素等条目**已删除**（不适用）。

- ✅ L1 绑手机：验证码（scene=6）校验 + `is_phone_verified=1`、`kyc_level=max(1,当前)`
- ✅ 级别单调不减（`max()`），不降级
- ✅ 已 ≥L1 时拒绝重复（`KycService::l1` → 14101）
- ✅ 真实姓名长度：2~32（`Validator` min/max，L2 接口保留）
- ⚠️ 身份证格式：15~32 位长度校验（docs/02 §6.3 口径；无 GB11643 算法——原清单条目已删除）
- ⚠️ 证件号存储：**明文落库 + 接口脱敏**（docs/04 §7.2 明确口径，与原清单「不落库」相反但符合本项目文档）
- ✅ 认证记录可追溯：每级一条 `kyc_records` + `provider` 标记
- ✅ KYC 接口登录鉴权（`/kyc/callback` 为第三方签名豁免，docs/02 §6.6）
- ✅ 验证码一次性消费

## 11. 安全基础与中间件

- ✅ CSRF 双提交：登录态写请求校验 `X-CSRF-Token`（= Access 签名片段）+ `hash_equals`，失败 419（`CsrfMiddleware`）
- ⚠️ CSRF Cookie 属性：非 HttpOnly、SameSite=Strict、TTL=access TTL（900s）（docs/04 §3.2；原清单 Lax/7 天按文档调整）
- ✅ 鉴权中间件：JWT 缺失/解析失败 → 401 JSON（`AuthMiddleware`）
- ⚠️ JWT payload：`uid/ver/did/iat/exp`，HS256，15 分钟（docs/01 §4.2；无 iss/jti——吊销依赖 `token_version`，无需黑名单）
- ✅ 密钥可配置：`JWT_SECRET`/`JWT_TICKET_SECRET`/`TOTP_ENCRYPTION_KEY` 等来自环境变量，`.env.example` 模板 + 生产门禁（docs/05 §3.4）
- ✅ 安全响应头：CSP/`X-Frame-Options: DENY`/`X-Content-Type-Options: nosniff`/`Referrer-Policy`/HSTS（`deploy/nginx/default.conf`；Permissions-Policy 未配——docs/04 §6.1 口径）
- ✅ 5xx 响应不返回堆栈（`AppExceptionHandler`，异常详情仅服务端日志）
- ✅ 未匹配路由 JSON 404（`HttpExceptionHandler` + 统一 JSON）
- ✅ 令牌 Cookie：httpOnly + SameSite=Strict + 生产 Secure
- ✅ 日志与响应不含明文密码/验证码（`mock_code` 仅 dev 门禁）
- ✅ 全部 SQL 走 ORM/查询构造器参数绑定，无字符串拼接
- ✅ bcrypt cost=12；验证码每行独立盐哈希

## 12. 通用检查（跨流程）

- ✅ 竞态原子性：唯一约束兜底（注册）、条件更新（验证码/恢复码/踢设备/轮换）、原子自增（失败计数）、`FOR UPDATE`（刷新轮换）
- ✅ 时区一致：全库 `BIGINT` Unix 秒（UTC），展示层格式化
- ✅ 输入归一化：`Validator::normalize` 统一 trim + 类型强转
- ✅ 无 N+1：设备名批量查询（`LogController::deviceNames` whereIn）
- ✅ 异常统一语义化状态码，兜底 500 JSON
- ⚠️ 多表写入事务：注册流程（用户 + 设备 + 登录日志）未包显式事务——单语句原子 + 失败可自愈（孤儿设备行可清理）；docs/01 D8「同事务」语义由单语句原子性满足（标注）
- ✅ 幂等性：唯一约束、验证码/state/票据一次性、条件更新防重复
- ✅ 业务阈值集中：`Constants` 枚举 + `AppErrorCode` + 环境变量（RATE_* / TTL_*），无散落魔法数字
- ✅ 注释与实现一致：加密列真实 AES-256-GCM（TOTP/第三方 token/PKCE）；「只追加」由 Aspect + 权限双重保障
- ✅ 敏感配置不入库（.gitignore + .env.example 模板）
- ✅ 错误文案不泄露内部细节

---

## 结论

- **通过项**：80+ 项，核心认证/会话/防爆破/审计/设备链路均满足文档契约
- **已知差异（均按本项目 docs 口径，非缺陷）**：锁定阈值 10 次、令牌 TTL（15 分/30 天）、SameSite=Strict、KYC 仅 L1、证件号明文落库+脱敏 等
- **已删除（不适用）**：QR 扫码登录、身份证 GB11643、银行卡 Luhn、活体照片上传、汇总统计接口、sms-codes 演示接口、X-Device-Id 头 等原系统条目
- **待评估项（docs 未要求）**：refresh 成功审计、429 审计、审计哈希链防篡改——按需扩展
